version: "3"
services:
  dev:
    image: wander_suite/db:dev
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - POSTGRES_DB="NotAValidDB"
      - POSTGRES_USER="NotAValidUser"
      - POSTGRES_PASSWORD="NotAValidPassword"
    ports:
      - "5432:5432"
    container_name: wander-db-dev
    volumes:
      - ./postgres/:/docker-entrypoint-initdb.d/